country_decisions = {
	embrace_chalcedonian = {
		potential = {
			num_of_cities >= 20
			religion = arian
		}
		allow = {
			stability >= 15
		}
		effect = {
				add_stability = -15
				set_country_religion = chalcedonian
				hidden_effect = {
					every_character = {
					limit = {
						has_religion = arian
					}
					set_character_religion = chalcedonian
				}
				}
			}		
		ai_will_do = {
			factor = 0
		}
	}
	}